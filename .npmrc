# Allow all build scripts
ignore-scripts=false

# Hoist dependencies to avoid script issues
shamefully-hoist=true

# Package-specific build script allowlist
execute-hooks=true
node-linker=hoisted

# Disable TTY requirement for module removal in CI/Docker
confirm-modules-purge=false
